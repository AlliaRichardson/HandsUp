<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="edu.eou.richarad.handsup">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".CreateAttendanceListActivity">
        </activity>
        <activity android:name=".AboutActivity">
        </activity>

        <receiver android:name=".MyBroadcastReceiver">
            <intent-filter>
                <action android:name="ALARM_BROADCAST"/>
            </intent-filter>
        </receiver>

        <activity android:name=".StudentRegisterActivity">
        </activity>
        <activity android:name=".TeacherRegisterStudentActivity">
        </activity>
        <activity android:name=".TeacherAttendanceActivity">
        </activity>
        <activity android:name=".StudentAttendanceActivity">
        </activity>
        <activity android:name=".AdminActivity">
        </activity>
        <activity android:name=".TeacherActivity">
        </activity>
        <activity android:name=".StudentActivity">
        </activity>
        <activity android:name=".LoginActivity">
        </activity>
        <activity android:name=".UpLoadUsersActivity">
        </activity>
        <activity android:name=".CourseSelectionDBActivity">
        </activity>
        <activity android:name=".ClassroomDBActivity">
        </activity>
        <activity android:name=".IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".AttendanceDBActivity"/>
    </application>

</manifest>